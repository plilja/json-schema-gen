{
    "type": "object",
    "required": ["kind"],
    "properties": {
        "kind": {"enum": ["premium", "basic"]}
    },
    "if": {
        "properties": {"kind": {"const": "premium"}}
    },
    "then": {
        "properties": {"discount": {"type": "integer", "minimum": 1, "maximum": 100}},
        "required": ["discount"]
    }
}
